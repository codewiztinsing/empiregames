version: "3.3"

services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: bingo_db
      DB_USER: postgres
      DB_PASSWORD: postgres
    depends_on:
      - postgres


    
